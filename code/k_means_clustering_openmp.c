#include "headers/k_means_clustering_openmp.h"
#include <stdio.h>

/* Placeholder: OpenMP implementation pending. */
cluster* kMeansOpenMP(observation observations[], size_t size, int k)
{
    printf("OpenMP implementation pending.\n");
    return NULL;
}

